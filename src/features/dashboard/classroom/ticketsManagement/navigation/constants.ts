/**
 * Tickets Management Feature - Constants
 *
 * Shared constants to avoid circular dependencies.
 */

import { CLASSROOM_PATH } from "../../navigation/constant";

export const TICKETS_BASE_PATH = `${CLASSROOM_PATH}/tickets-management`;
