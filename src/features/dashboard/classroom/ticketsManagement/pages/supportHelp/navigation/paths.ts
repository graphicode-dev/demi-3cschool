import { CLASSROOM_PATH } from "../../../../navigation/constant";

/**
 * Support Help paths
 */
export const supportHelp = {
    root: () => `${CLASSROOM_PATH}/support-help`,
    create: () => `${CLASSROOM_PATH}/support-help/create`,
};

export const supportHelpPaths = {
    supportHelpRoot: supportHelp.root,
    supportHelpCreate: supportHelp.create,
};
