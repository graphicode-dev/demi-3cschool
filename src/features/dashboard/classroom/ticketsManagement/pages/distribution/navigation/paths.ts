import { TICKETS_BASE_PATH } from "../../../navigation/constants";

/**
 * Distribution paths
 */
export const distribution = {
    root: () => `${TICKETS_BASE_PATH}/distribution`,
};

export const distributionPaths = {
    distributionRoot: distribution.root,
};
