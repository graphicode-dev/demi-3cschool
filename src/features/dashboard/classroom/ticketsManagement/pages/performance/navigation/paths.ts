import { TICKETS_BASE_PATH } from "../../../navigation/constants";

/**
 * Performance paths
 */
export const performance = {
    root: () => `${TICKETS_BASE_PATH}/performance`,
};

export const performancePaths = {
    performanceRoot: performance.root,
};
