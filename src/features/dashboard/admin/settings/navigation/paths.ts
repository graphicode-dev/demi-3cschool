import { slotsPaths } from "../slots";
import { studentsPaths } from "../students";
import { teachersPaths } from "../teachers";
import { trainingCentersPaths } from "../trainingCenters";

export const settingsPaths = {
    slotsPaths,
    studentsPaths,
    teachersPaths,
    trainingCentersPaths,
} as const;
