import { TICKETS_BASE_PATH } from "../../../navigation/constants";

/**
 * Overview paths
 */
export const overview = {
    root: () => `${TICKETS_BASE_PATH}/overview`,
};

export const overviewPaths = {
    overviewRoot: overview.root,
};
